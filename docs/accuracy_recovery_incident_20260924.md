# EncBank accuracy continuation: 2026-09-24 22:48 +08:00

User authorization: resubmit unscored Terminal-Bench tasks, increase concurrency, and prioritize accuracy over reproducing the prior environment qualification. Model weights and adapters remain on the server. Dense remains closed at 89/89; old verified k12 and k48 baselines were 30/89 and 52/89.

The first accuracy-only batch was k12 140587–140591 and k48 140592–140594. Job 140587 on lj-gpu6 launched qemu-alpine-ssh and 11 other tasks; the 11 others failed before any model call because direct APT access was unavailable. The qemu trial made model requests but obtained no verifier score: the worker failed on BeeGFS `Remote I/O error` while creating a Triton cache directory, then controller session release failed. Its task parent wait is preserved as an infrastructure interruption, not a score. Jobs 140588–140594 had zero model requests and were cancelled after confirming this. The original root and evidence remain unchanged.

The proxy-enabled successor batch k12 141032–141036 and k48 141037–141039 partitions 95 unscored tasks, excluding the then-active qemu trial. Source snapshots, task ownership, and submission records are immutable; registry v4 is preserved. The authorized proxy `PROXY_HOST:8888` responds from gpu1/gpu6/gpu7; these new jobs excluded observed-stalled lj-gpu1. User then explicitly allowed gpu7. The Slurm reservation `SITE_GPU_RESERVATION` lists the current account and was assigned to the 11 main/Hot pending jobs using the user's own account. The gpu7 filesystem probe passed and seven main jobs reached model-ready with actual requests and replies. k12 portfolio-optimization returned a valid verifier reward 1.0, taking known k12 to at least 31/89. Its result and parent evidence remain in the original successor root. Several other tasks failed before model calls because Ubuntu APT security mirrors served inconsistent index/package versions; those are infrastructure failures, not zero scores.

The seven running proxy jobs were paused for **new task admission only**, then given the source's native DRAIN signal. Their already-started trials were not cancelled. The by-hash successor sources were frozen for 63 unlaunched or clearly identified zero-model-call APT failures, excluding all launched active trials. Registry v5 assigns those 63 tasks uniquely to eight new roots. A real Apptainer bind test verified `Acquire::By-Hash "force"` inside a task image, and the repository's exact by-hash SHA URLs answered correctly. The fresh roots also request 24 CPU per GPU, with no model/task/token time bound. The one-shot capped dispatcher would admit at most nine main running-plus-pending GPU requests as drained jobs exit. It submitted only k12 by-hash job **141233**, which failed in two seconds with zero model calls while creating its runtime directory; the dispatcher was stopped. No other by-hash root has been submitted. Do not restart 141233 or the dispatcher without fresh reconciliation.

The independent Hot89 series retained four valid old results. After the old dead-worker job 128897 was cancelled, Hot jobs 140658–140660 entered gpu1 but stalled during model loading on BeeGFS; all were cancelled before formal trial launches or model calls. Successors 141091/141077/141078 entered gpu7; two reached model startup but failed before calls because their immutable source snapshot omitted `peft_preflight.json`, while the third installation qualification hit the same APT mirror-sync error. All three ended with zero formal calls. New source snapshots include the PEFT receipt and, under the user's accuracy-only override, skip only the install-only qualification. Their jobs **141152–141154** were submitted with the authorized gpu7 reservation, but Slurm reported `FAILED 0:53` immediately before any trial. These IDs are closed; do not resubmit them blindly. Hot retains 4/89 scored.

The current hard blocker is cluster BeeGFS metadata instability across gpu1 and gpu7, independent of task score or the original Docker issue. At 22:37–22:39 gpu7 kernel logs include `MkDirResp ... Internal error`, `FhgfsOps_rename ... Internal error`; job 141233 received `OSError(121, 'Remote I/O error')` creating `/cluster/home/USER/qencbank_runtime_20260911/server_control_20260920/k12_byhash_recovery_1_20260924/rpc`. The old qemu worker received the same error creating a Triton cache directory. This requires cluster storage service repair or stable shared storage before reliable resubmission. An APT mirror gate was stopped after repeated hash mismatches and superseded by by-hash source snapshots; it never submitted tasks. Do not mistake the source-only by-hash preparation or Slurm allocation for scored inference.

At this timestamp, main jobs 141032, 141036 and 141038 were still RUNNING and natively draining their already-launched trials. 141033, 141034, 141035 and 141037 had FAILED with preserved evidence. The remaining unscored launched tasks need reconciliation after storage repair; do not count infrastructure interruptions as normal score zero or duplicate any valid score. The updated hourly observer should continue, with no automatic resubmission while BeeGFS returns EIO.
